# ── Streamlit Cloud deployment requirements ───────────────────────────────────
# Only packages needed to RUN the website are listed here.
# Training packages (biopython, scipy, matplotlib, seaborn, jupyter) are in
# requirements-train.txt — install those locally when retraining models.

streamlit==1.37.0
plotly==5.23.0
pandas==2.2.2
numpy==1.26.4
requests==2.32.3
scikit-learn==1.5.1
xgboost==2.1.1
fpdf2==2.8.1
shap==0.46.0
